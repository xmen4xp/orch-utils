# SPDX-FileCopyrightText: 2025 Intel Corporation
#
# SPDX-License-Identifier: Apache-2.0
---
{{- range .Values.controlplane.providers }}
apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: ControlPlaneProvider
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
spec:
  {{- toYaml .spec | nindent 2 }}
---
{{- if .additionalManifests }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .additionalManifests.name }}
  namespace: {{ .additionalManifests.namespace | default .namespace }}
data:
  manifests: |
    {{- if .additionalManifests.manifests }}
    {{- toYaml .additionalManifests.manifests | nindent 4 }}
    {{- end }}
    {{- if .Values.metrics.enabled }}
    ---
    {{- include "metricsservicetemplate" (dict "name" (printf "%s-%s" "controlplane" .name) "namespace" .namespace ) | nindent 4 }}
        selector:
          cluster.x-k8s.io/provider: control-plane-{{ .name }}
    ---
    {{- include "metricsservicemonitortemplate" (dict "name" (printf "%s-%s" "controlplane" .name) "namespace" .namespace) | nindent 4 }}
    {{- end }}
{{- end }}
---
{{- end }}
